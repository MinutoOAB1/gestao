{
  "buildCommand": "npm run build",
  "outputDirectory": "frontend/dist",
  "framework": "vite",
  "functions": {
    "api_server.ts": {
      "memory": 1024,
      "maxDuration": 15,
      "includeFiles": [
        "node_modules/.prisma/client/**/*",
        "backend/prisma/schema.prisma",
        "backend/dist/**/*"
      ]
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api_server.ts"
    }
  ]
}
