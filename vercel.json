{
  "buildCommand": "npm run build",
  "outputDirectory": "frontend/dist",
  "framework": "vite",
  "functions": {
    "api/server.ts": {
      "memory": 3008,
      "maxDuration": 15,
      "includeFiles": "{node_modules/.prisma/client/**/*,backend/prisma/schema.prisma,backend/dist/**/*,backend/node_modules/**/*}"
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/server.ts"
    }
  ]
}
